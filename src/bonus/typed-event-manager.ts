import { EventEmitter } from "events";

const emitter = new EventEmitter();

// Typed Event Management Exercise

// dispatch(new Event(props))
// addListener(EventClass, (e) => {
// autocompletion for e
// })

// TO BUILD FROM GROUND UP
